import type { Info } from "../schemas";

export const mobileBreakpoint = 768;

export const defaultInfoData: Info = {
    page: 1,
    limit: 20,
    results: 0,
    total: 0,
    nextPage: null,
    previousPage: null,
};
